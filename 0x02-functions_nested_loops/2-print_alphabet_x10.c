#include 'holberton.h'
/**
 *
 * print 10 times the alphabet
 * 
 **/
void print_alphabet_x10(void);
{
    int i = 0;
    char alpha;

    while ( i < 10)
{
    alpha = a
    while (a <= 'z')
{
    _putchar(alpha)
    alpha++
}
    _putchar('/n')
    i++
}
}